- if @current_user.is_child?
	= render 'children/banner'
	= render 'children/nav'
- else
	= render 'users/banner'
	= render 'users/nav'

.journal_entry
	%h3= @entry.title
	.journal_content
		= @entry.content.html_safe
	- for sticking in @entry.stickings
		%div[sticking]{ :style => "top:#{sticking.top}px; left:#{sticking.left}px;" }
			= image_tag sticking.sticker.avatar.location


- unless @entry.comments.empty?
	#comments
		%h3 Comments
		= render @entry.comments
#comment
	= render 'comments/new'