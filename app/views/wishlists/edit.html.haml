- content_for :left_column do
	%h1 Editing Wishlist
	= link_to 'Show', @wishlist

	= form_for @wishlist , :html => { :mutipart => true } do |f|
		= render 'form', :f => f
		.actions
			= f.submit 'Update', :class => 'button'
			|
			= link_to 'Cancel', :back



