- content_for :left_column do
	%h1 Editing Quiz
	= link_to 'Show', @quiz
	= form_for @quiz , :html => { :mutipart => true } do |f|
		= render 'form', :f => f
		.actions
			= f.submit 'Update', :class => 'button'
			|
			= link_to 'Cancel', :back
