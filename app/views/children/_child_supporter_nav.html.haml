.pic
	= avatar_tag( child, :thumb, :width => 50 )
.name_nav
	.name
		= link_to child.display_name, child
	.nav
		= link_to "Journal", child_journal_entries_path( child )
		|
		= link_to "Profile", profile_child_path( child )
		|
		= link_to "Send Note", new_note_path( :to => child.id )
%hr.clear