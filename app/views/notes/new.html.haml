- if @current_user.is_child?
	= render 'child/banner'
	= render 'children/nav'
- else
	= render 'adult/nav'
	= render 'child/sub_nav', :child => @child
.cork_board
	.note_detail.schoolbell
		.note_title
			Write a Note!
		= form_for @note do |f|
			= render 'form', :f => f
