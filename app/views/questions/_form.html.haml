.form_group
	%p
		= f.label 'Question:'
		%br
		= link_to_remove_fields( "Remove Question", f )
		= f.text_area :content
	

	= f.fields_for :answers do |builder|
		= render 'answers/form', :f => builder
	
	= link_to_add_fields( "Add Answer", f, :answers )
