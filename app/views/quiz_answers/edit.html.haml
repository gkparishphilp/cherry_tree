%h1 Editing Answer
= link_to 'Show', @answer

= form_for @answer , :html => { :mutipart => true } do |f|
	= render 'form', :f => f
	.actions
		= f.submit 'Update', :class => 'button'
		|
		= link_to 'Cancel', :back



