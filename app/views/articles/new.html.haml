- content_for :left_column do
	.form_group
		%h1 
			New Blog Article
		= form_for [@current_author, @article], :html => { :multipart => true } do |f|
			= render 'form', :f => f, :m => 'Create'
		
- content_for :right_column do
	= render 'admin/nav'

